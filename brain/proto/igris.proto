syntax = "proto3";

package igris;

message IgrisMessage {
  string type = 1;
  string source = 2;
  string target = 3;
  string payload = 4;
}

service CoreRouter {
  rpc Connect(stream IgrisMessage) returns (stream IgrisMessage);
}
